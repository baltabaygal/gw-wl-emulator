# notebooks/ — Jupyter notebooks

Exploratory Jupyter notebooks. Run with the `test` conda env (has the `gwlensing`
module, ACE, and the ipykernel from `requirements.txt`).

- `inspect_dataset.ipynb` — interactive inspection of generated datasets.

Notebooks are for exploration; promote any reusable logic into `python/` or `ml/`.
