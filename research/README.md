# research/ — Exploratory / experimental work

Scratch space for one-off experiments and paper-figure explorations that haven't been
promoted into `scripts/`, `ml/`, or `paper_prod/`.

- `subhalo_factor_truthd_paper_b.{py,pdf,png}` — a subhalo-factor "truth-d" paper panel.

⚠️ **Status:** small and mostly superseded — the canonical subhalo-factor paper plots now
live in `paper_prod/`. This folder is a candidate for archiving; keep it only if you still
need the exploratory version. Nothing in the tracked codebase imports from here.
